services:
  imageresizerfunc:
    container_name: ImgResizerAzureFunc
    image: ${DOCKER_REGISTRY-}imageresizerfunc
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
        - .env.docker
    ports:
     - 6000:80
        

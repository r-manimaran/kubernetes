apiVersion: v1
kind: Service
metadata:
  name: tracingapp
  namespace: sidecar-tracing
spec:
  selector:
    app: tracingapp-deployment
  ports:
  - name: http
    port: 80
    targetPort: 8080
  - name: metrics
    port: 9090
    targetPort: 9090
  type: ClusterIP
